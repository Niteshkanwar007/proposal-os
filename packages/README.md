# packages/

Holds modular, reusable packages and libraries consumed by the apps in this monorepo. Each package should be focused, well-documented, and self-contained (parser, observer, analyst, etc.). Use this directory for shared business logic, utilities, and domain libraries rather than placing them inside apps/.
