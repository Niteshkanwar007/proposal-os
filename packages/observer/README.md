# observer

Implements observation and telemetry concerns: collecting, normalizing, and exposing system observations. Use this package to capture runtime signals, logs, and environment context for downstream analysis. It should provide interfaces and adapters to fetch and forward observations consistently.
