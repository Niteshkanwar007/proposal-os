# core

Core types, interfaces, and shared domain models used across the monorepo. This package centralizes foundational contracts and abstractions so other packages can remain decoupled. Keep only stable, low-level primitives here.
