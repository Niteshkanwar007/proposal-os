# parser

Responsible for input ingestion and parsing logic across the system. This package should expose utilities for tokenization, normalization, and converting raw inputs into structured data consumed by other packages. Keep parsing concerns isolated so other packages can rely on stable parsed representations.
