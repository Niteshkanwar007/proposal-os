# apps/

This directory holds application projects for the monorepo. Each subfolder is an independent, deployable application (for example, web and api). Keep application-specific configuration, deployment and packaging concerns here. Reusable code should live in the packages/ directory, not in apps/.
