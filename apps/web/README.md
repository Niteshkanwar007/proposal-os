# web

Frontend web application. This folder contains the user-facing client application, static assets, and frontend-specific configuration. Implementation details (frameworks, routing, styles) and build outputs belong here. Shared code should be imported from packages/.
