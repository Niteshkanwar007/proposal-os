# api

Backend API service. This folder contains the service exposing programmatic endpoints and server-side logic. Keep API routing, controllers, and deployment configuration here. Reusable libraries and domain logic belong in packages/.
